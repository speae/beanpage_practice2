<!DOCTYPE HTML>
<html>
<HEAD>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%=title%></title>
    <!-- Bootstrap CDN -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.0/js/bootstrap.min.js"></script>

    <style>
        .top > ul > li {
            float: left;
            margin-left: 100px;
        }

        .start li {
            list-style: none;
        }

        .start li a {
            text-decoration: none;
        }

        .title {
            background-color: aqua;
        }

        .top {
            background-color: aquamarine;
            height: 50px;
        }

        table {
            margin-left: 500px;
        }

        td {
            width: 200px;
            height: 50px;
        }
    </style>
    <div class="title">
        게시판
        <nav class="top">
            <ul class="start">
                <li><a href="/board/list">게시판</a></li>
                <li><a href="/site">홈으로</a></li>
            </ul>
        </nav>
    </div>
</HEAD>
<BODY>
<script>
   function check() {
       const question = confirm('삭제하시겠습니까?');
       if(question === true){
           location.href="/board/delete?num=<%=rows.num%>";
       }else{
           alert('취소하였습니다.')
           location.href="/board/read?num=<%=rows.num%>";
       }
   }
</script>
<table>
        <tr>
            <td>번호</td><td><input type="text" name="num" value="<%=rows.num%>" readonly></td>
        </tr>
        <tr>
            <td>작성일</td><td><input type="text" name="write_date" value="<%=rows.write_date%>" readonly></td>
        </tr>
        <tr>
            <td>조회수</td><td><input type="text" name="count" value="<%=rows.count%>" readonly></td>
        </tr>
        <tr>
            <td>제목</td><td><input type="text" name="subject" value="<%=rows.subject%>" readonly></td>
        </tr>
        <tr>
            <td>글쓴이</td><td><input type="text" name="writer" value="<%=rows.writer%>" readonly></td>
        </tr>
        <tr>
            <td>내용</td><td><textarea cols="23" rows="15" name="write_content"  readonly><%=rows.write_content%></textarea></td>
        </tr>
        <tr>
            <td><button onclick="location.href='/board/update?num=<%=rows.num%>'">수정</button></td>
            <td><button onclick="check()">삭제</button></td>
        </tr>
    </table>

<button onclick="location.href='/board/list'">돌아가기</button>
</BODY>
<footer>

</footer>
</html>